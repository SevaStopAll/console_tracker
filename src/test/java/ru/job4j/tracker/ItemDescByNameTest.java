package ru.job4j.tracker;

import org.junit.jupiter.api.Test;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import static org.assertj.core.api.Assertions.assertThat;

/*
class ItemDescByNameTest {
    @Test
    void when3ItemsSorted() {
        List<Item> result = new ArrayList<>();
        result.add(new Item("Report to IT support", 12));
        result.add(new Item("Call manager", 21));
        result.add(new Item("Go home soon", 1));
        List<Item> expected = new ArrayList<>();
        expected.add(new Item("Report to IT support", 12));
        expected.add(new Item("Go home soon", 1));
        expected.add(new Item("Call manager", 21));
        result.sort(new ItemDescByName());
        assertThat(result).isEqualTo(expected);
    }

    @Test
    void when5ItemsSorted() {
        Item[] items = {new Item("Report to IT support", 12), new Item("Call manager", 21), new Item("Go home soon", 1),
                new Item("Do sport", 9), new Item("Have fun", 8) };
        List<Item> result = Arrays.asList(items);
        Item[] items2 = {new Item("Report to IT support", 12), new Item("Have fun", 8),
                new Item("Go home soon", 1),  new Item("Do sport", 9), new Item("Call manager", 21)};
        List<Item> expected = Arrays.asList(items);
        result.sort(new ItemAscByName());
        assertThat(result).isEqualTo(expected);
    }
}*/
